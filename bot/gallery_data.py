# This file will store gallery images and votes in memory for now.
gallery_images = (
    []
)  # List of dicts: { 'url': str, 'uploader': int, 'votes': set(user_id) }
# changings this sqlLITe soon once uploaded to AWS
